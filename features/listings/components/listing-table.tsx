import Link from "next/link";
import { StatusBadge } from "@/components/shared/status-badge";
import { listingRows } from "./listing-data";

export function ListingTable() {
  return <div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[1050px] text-left"><thead className="bg-slate-50/80 text-[11px] font-semibold tracking-wide text-slate-400"><tr><th className="px-6 py-3.5">건물명 / 호실</th><th className="px-4 py-3.5">구조</th><th className="px-4 py-3.5">보증금 / 월세</th><th className="px-4 py-3.5">관리비</th><th className="px-4 py-3.5">입주 가능일</th><th className="px-4 py-3.5">매물 상태</th><th className="px-4 py-3.5">광고 채널</th><th className="px-6 py-3.5 text-right">작업</th></tr></thead><tbody className="divide-y divide-slate-100">{listingRows.map((listing) => <tr key={listing.id} className={`text-sm hover:bg-slate-50/70 ${listing.status === "계약완료" ? "opacity-60" : ""}`}><td className="px-6 py-4"><Link href={`/listings/${listing.id}`} className="font-semibold text-slate-800 hover:text-blue-600 hover:underline">{listing.building} {listing.unit}</Link><p className="mt-1 text-[11px] text-slate-400">{listing.address}</p></td><td className="px-4 py-4 text-slate-600">{listing.structure}</td><td className="px-4 py-4 font-mono font-semibold tabular-nums text-slate-800">{listing.price}</td><td className="px-4 py-4 font-mono tabular-nums text-slate-600">{listing.fee}</td><td className={`px-4 py-4 ${listing.available === "즉시 입주" ? "font-medium text-emerald-600" : "text-slate-500"}`}>{listing.available}</td><td className="px-4 py-4"><StatusBadge label={listing.status} /></td><td className="px-4 py-4 text-slate-500">{listing.channels}</td><td className="px-6 py-4 text-right"><div className="flex justify-end gap-1.5"><Link href={`/listings/${listing.id}`} className="rounded-lg bg-slate-100 px-2.5 py-1.5 text-[11px] font-semibold text-slate-700 hover:bg-slate-200">상세보기</Link><button type="button" disabled className="rounded-lg bg-blue-50 px-2.5 py-1.5 text-[11px] font-semibold text-blue-700 opacity-60">빠른수정</button></div></td></tr>)}</tbody></table></div>;
}
