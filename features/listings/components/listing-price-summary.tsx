import { SensitiveInfo } from "@/components/shared/sensitive-info";
import { listingDetails } from "./listing-detail-data";

export function ListingPriceSummary() {
  return <section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm shadow-slate-200/40 sm:p-6"><div className="flex flex-wrap items-end justify-between gap-4 border-b border-slate-100 pb-5"><div><span className="rounded-md bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">월세 조건</span><p className="mt-3 font-mono text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">{listingDetails.price}</p></div><div className="text-left sm:text-right"><p className="text-xs text-slate-500">관리비</p><p className="mt-1 font-mono text-base font-semibold text-slate-800">{listingDetails.maintenance}</p></div></div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-4">{listingDetails.specs.map(([label, value]) => <div key={label} className="rounded-xl border border-slate-100 bg-slate-50 p-3"><p className="text-[11px] text-slate-500">{label}</p><p className={`mt-1 text-xs font-semibold ${label === "입주 가능일" ? "text-emerald-600" : "text-slate-800"}`}>{value}</p></div>)}</div><div className="mt-5"><h2 className="text-xs font-semibold text-slate-700">기본 제공 옵션</h2><div className="mt-2 flex flex-wrap gap-2">{listingDetails.options.map((option) => <span key={option} className="rounded-lg bg-slate-100 px-2.5 py-1.5 text-xs font-medium text-slate-700">{option}</span>)}</div></div><div className="mt-5"><SensitiveInfo title="내부 중개 메모"><p className="text-xs leading-5 text-slate-600">가공 메모입니다. 실제 내부 메모는 권한이 있는 조직 사용자만 상세 화면에서 확인할 수 있습니다.</p></SensitiveInfo></div></section>;
}
