import { FlowGuide } from "@/features/property-types/components/property-type-preview-workspace";
import { propertyGroups, type PropertyGroup } from "@/features/property-types/mock-data";
import Link from "next/link";

const fieldMap: Record<PropertyGroup, string[]> = { residential: ["건물명", "호실", "방 구조", "보증금·월세", "입주 조건"], apartment: ["단지명", "동·호수", "전용면적", "보증금·월세 또는 매매가", "주차·관리비"], officetel: ["건물명·호실", "전용면적", "주거/업무 용도", "관리비", "주차"], commercial: ["건물명·호실", "전용면적", "보증금·월세", "권리금", "업종 제한·부가세"] };
export function PropertyTypeRegistrationPreview({ selected, basePath = "/property-types" }: { selected: string; basePath?: string }) { const group = propertyGroups.some((item) => item.id === selected) && selected !== "all" ? selected as PropertyGroup : "apartment"; const label = propertyGroups.find((item) => item.id === group)?.label ?? "아파트"; return <><FlowGuide active="등록" /><section className="max-w-3xl rounded-xl border border-[#e5e1db] bg-white"><div className="border-b border-[#e5e1db] p-5"><h2 className="font-extrabold">{label} 신규 등록 미리보기</h2><p className="mt-1 text-xs text-[#77736e]">가공 화면입니다. 실제 매물·DB에는 저장되지 않습니다.</p></div><div className="grid gap-4 p-5 sm:grid-cols-2">{fieldMap[group].map((field) => <label key={field}><span className="label">{field}</span><input className="field" placeholder={`${field} 입력`} /></label>)}</div><div className="flex justify-between border-t border-[#e5e1db] p-5"><Link className="rounded-lg border px-3 py-2 text-xs font-bold" href={`${basePath}?type=${group}`}>목록으로</Link><Link className="rounded-lg bg-[#3e3a37] px-3 py-2 text-xs font-bold text-white" href="/consultations">상담·계약 공통 흐름 보기</Link></div></section></>; }
