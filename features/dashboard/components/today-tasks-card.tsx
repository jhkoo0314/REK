import { CheckCircle2, ChevronRight } from "lucide-react";
import { todayTasks } from "./dashboard-data";

export function TodayTasksCard() {
  return <article className="rounded-2xl border border-slate-200/80 bg-white shadow-sm shadow-slate-200/40"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-5"><div><h2 className="font-semibold text-slate-950">오늘 할 일</h2><p className="mt-1 text-xs text-slate-400">중요한 순서대로 확인하세요.</p></div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">3건 남음</span></div><ul className="divide-y divide-slate-100 px-5">{todayTasks.map((task) => <li key={task.title} className="flex items-center gap-3 py-4"><span className={`grid size-5 shrink-0 place-items-center rounded-full border ${task.done ? "border-blue-500 bg-blue-500 text-white" : "border-slate-300 bg-white"}`}>{task.done ? <CheckCircle2 className="size-3.5" /> : null}</span><div className="min-w-0 flex-1"><p className={`truncate text-sm font-medium ${task.done ? "text-slate-400 line-through" : "text-slate-700"}`}>{task.title}</p><p className="mt-1 text-xs text-slate-400">{task.category}</p></div><time className={`text-xs font-medium ${task.done ? "text-slate-400" : "text-slate-600"}`}>{task.time}</time></li>)}</ul><button type="button" className="flex w-full items-center justify-center gap-1 border-t border-slate-100 py-4 text-sm font-semibold text-slate-500 hover:bg-slate-50 hover:text-blue-700">업무 관리 준비 중 <ChevronRight className="size-4" /></button></article>;
}
