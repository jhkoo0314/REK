"use client";

import { Check, Copy, RefreshCw, Sparkles } from "lucide-react";
import { useState } from "react";
import { channelTemplates, type ChannelName } from "./advertisement-data";

const keywords = ["남향", "즉시 입주", "깔끔한 상태", "주차 편리"];

export function CopyTemplateStudio() {
  const [channel, setChannel] = useState<ChannelName>("네이버 부동산");
  const [selectedKeywords, setSelectedKeywords] = useState(new Set(["남향", "즉시 입주", "깔끔한 상태"]));
  const [copied, setCopied] = useState(false);
  const template = channelTemplates[channel];
  const toggleKeyword = (keyword: string) => setSelectedKeywords((current) => { const next = new Set(current); if (next.has(keyword)) next.delete(keyword); else next.add(keyword); return next; });
  const copyTemplate = async () => { try { await navigator.clipboard.writeText(`${template.title}\n\n${template.body}`); setCopied(true); setTimeout(() => setCopied(false), 1800); } catch { setCopied(false); } };

  return <section className="grid gap-6 lg:grid-cols-2"><article className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm shadow-slate-200/40 sm:p-6"><header className="border-b border-slate-100 pb-4"><h2 className="flex items-center gap-2 font-semibold text-slate-900"><Sparkles className="size-5 text-blue-600" /> 광고 문구 템플릿</h2><p className="mt-1 text-xs leading-5 text-slate-500">채널에 맞는 문구 초안을 만들고 담당자가 직접 수정·확인합니다.</p></header><div className="mt-5"><label className="mb-1.5 block text-xs font-semibold text-slate-700">대상 매물</label><select className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 text-sm font-medium text-slate-700"><option>테스트빌 101호 · 월세 3,000 / 80 · 공실</option><option>샘플하우스 202호 · 월세 1,500 / 65</option></select></div><div className="mt-5"><p className="mb-1.5 text-xs font-semibold text-slate-700">게시 채널 톤</p><div className="grid grid-cols-3 gap-2">{(Object.keys(channelTemplates) as ChannelName[]).map((name) => <button key={name} type="button" onClick={() => setChannel(name)} className={`rounded-xl border px-2 py-3 text-center text-xs transition ${channel === name ? "border-blue-600 bg-blue-50 font-semibold text-blue-700" : "border-slate-200 bg-slate-50 text-slate-600 hover:bg-slate-100"}`}><span className="block">{name}</span><span className="mt-1 block text-[10px] font-normal opacity-70">{channelTemplates[name].subtitle}</span></button>)}</div></div><div className="mt-5"><p className="mb-2 text-xs font-semibold text-slate-700">강조할 키워드</p><div className="flex flex-wrap gap-2">{keywords.map((keyword) => <button key={keyword} type="button" onClick={() => toggleKeyword(keyword)} className={`rounded-lg px-3 py-1.5 text-xs font-medium ${selectedKeywords.has(keyword) ? "bg-blue-100 text-blue-800" : "bg-slate-100 text-slate-600"}`}>{selectedKeywords.has(keyword) ? "✓ " : ""}{keyword}</button>)}</div></div><p className="mt-6 rounded-xl bg-blue-50 px-3 py-3 text-xs leading-5 text-blue-700">문구 템플릿 선택·복사만으로 광고 게시 상태나 매물 상태는 변경되지 않습니다.</p></article><article className="flex flex-col rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm shadow-slate-200/40 sm:p-6"><header className="flex items-start justify-between gap-3 border-b border-slate-100 pb-4"><div><h2 className="font-semibold text-slate-900">문구 미리보기</h2><p className="mt-1 text-xs text-slate-500">{channel}용 가공 템플릿</p></div><span className="rounded-md bg-emerald-100 px-2 py-1 text-[10px] font-semibold text-emerald-800">초안 준비됨</span></header><div className="mt-4 flex-1 rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm leading-6 text-slate-700"><h3 className="font-semibold text-slate-950">{template.title}</h3><p className="mt-4 whitespace-pre-line">{template.body}</p></div><div className="mt-4 flex gap-2"><button type="button" className="inline-flex items-center gap-1.5 rounded-xl bg-slate-100 px-3 py-2.5 text-xs font-semibold text-slate-600"><RefreshCw className="size-3.5" /> 템플릿 다시 선택</button><button type="button" onClick={copyTemplate} className="inline-flex flex-1 items-center justify-center gap-2 rounded-xl bg-blue-600 px-3 py-2.5 text-xs font-semibold text-white"><Copy className="size-3.5" /> {copied ? <><Check className="size-3.5" /> 복사됨</> : "전체 복사"}</button></div></article></section>;
}
