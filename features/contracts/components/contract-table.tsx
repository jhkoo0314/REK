import { StatusBadge } from "@/components/shared/status-badge";
import { contractRows } from "./contract-data";

export function ContractTable() {
  return <div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[920px] text-left"><thead className="bg-slate-50/80 text-[11px] font-semibold tracking-wide text-slate-400"><tr><th className="px-6 py-3.5">계약 매물 / 호실</th><th className="px-5 py-3.5">임차인</th><th className="px-5 py-3.5">보증금 / 월세</th><th className="px-5 py-3.5">계약 기간</th><th className="px-5 py-3.5">남은 기간</th><th className="px-5 py-3.5">상태</th><th className="px-6 py-3.5 text-right">관리</th></tr></thead><tbody className="divide-y divide-slate-100">{contractRows.map((row) => <tr key={row.id} className={`text-sm hover:bg-slate-50/70 ${row.urgent ? "bg-rose-50/25" : ""}`}><td className="px-6 py-4"><p className="font-semibold text-slate-800">{row.property}</p><p className="mt-1 text-[11px] text-slate-400">{row.type}</p></td><td className="px-5 py-4 font-medium text-slate-700">{row.tenant}</td><td className="px-5 py-4 font-mono font-semibold text-slate-800">{row.price}</td><td className="px-5 py-4 font-mono text-xs text-slate-600">{row.period}</td><td className="px-5 py-4"><span className={`rounded-md px-2.5 py-1 text-xs font-semibold ${row.urgent ? "bg-rose-100 text-rose-700" : "bg-slate-100 text-slate-600"}`}>{row.remaining}</span></td><td className="px-5 py-4"><StatusBadge label={row.status} /></td><td className="px-6 py-4 text-right"><button type="button" disabled className="rounded-lg bg-slate-100 px-2.5 py-1.5 text-[11px] font-semibold text-slate-600 opacity-60">상세 보기</button></td></tr>)}</tbody></table></div>;
}
